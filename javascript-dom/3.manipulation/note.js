/*
Manipulation

    Element Manipulation Method 
    Note : Memanipulasi sebuah elemen yang sudah ada.

    - element.innerHTML : Merubah isi dari tag yang diseleksi
    - element.style.<property> : Merubah style dari tag yang diseleksi
    - element.setAttribute() : Memanipulasi atribut sebuah elemen
    - element.classList : Memanipulasi class sebuah elemen

    Node Manipulation Method
    Note : Menambahkan, menghapus, mengganti sebuah elemen.

    - document.createElement() : Membuat node elemen baru
    - document.createTextNode() : Membuat isi dari node
    - node.appendChild() : Menggabungkan antar node
    - node.insertBefore() : Menggabungkan node di tengah tengah
    - parentNode.removeChild() : 
    - parentNode.replaceChild() :
*/